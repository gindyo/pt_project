@model PTProject.Models.Searchable

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Searchable</legend>

    <div class="display-label">content</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.content)
    </div>

    <div class="display-label">type</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.type)
    </div>

    <div class="display-label">title</div>
    <div class="display-field">
        @Html.DisplayFor(model => SearchablesHelper.pretty_type(model.type))
    </div>

    <div class="display-label">Unit</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Unit.title)
    </div>

    <div class="display-label">search_term</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.search_term)
    </div>

    <div class="display-label">link</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.link)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
